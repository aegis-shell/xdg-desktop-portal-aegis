# Compat shim for the classic Secret Service API. Transitional — scheduled
# for removal together with the secret::compat module. Activates the same
# all-in-one portal process.
[D-BUS Service]
Name=org.freedesktop.secrets
Exec=/usr/lib/xdg-desktop-portal-aegis
