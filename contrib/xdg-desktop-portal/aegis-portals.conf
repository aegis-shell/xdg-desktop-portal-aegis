[preferred]
# aegis serves Settings, Screenshot, ScreenCast, Background, and Inhibit
# (ADR-0051, ADR-0053); every other interface falls back to the GTK
# backend, which provides the UI-driven portals
# (file chooser, app chooser, notifications, …) until aegis grows its own.
default=aegis;gtk
